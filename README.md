# SPARC

**S**uper**P**ixel-**A**ware **R**egion **C**ontrastive learning for self-supervised dense prediction.

> **Status: under construction.** The method code is in place and installable; the
> CLI, downstream evaluation, docs and model zoo land in subsequent phases.
> See `docs/` (forthcoming) for the full guide.

## Where is…?

| I want to understand… | Go to |
|---|---|
| The region contrastive loss | [`src/sparc/losses/region_contrastive.py`](src/sparc/losses/region_contrastive.py) |
| Superpixel region pooling | [`src/sparc/models/region_pooling.py`](src/sparc/models/region_pooling.py) |
| Aligned image/mask augmentation | [`src/sparc/data/transforms.py`](src/sparc/data/transforms.py) |
| The image+mask dataset | [`src/sparc/data/mask_dataset.py`](src/sparc/data/mask_dataset.py) |
| How superpixel masks are generated | [`src/sparc/data/superpixel/`](src/sparc/data/superpixel/) |
| **Where my datasets are** | `configs/paths.yaml` — the only file you must edit |

## Install

```bash
pip install -e ".[dev]"
pytest                      # fast, CPU-only, no datasets needed
```

## What this repository does and does not contain

It contains code, configs and experiment manifests.

It contains **no datasets, no superpixel masks and no model weights**. Masks are
generated by `sparc-masks`; pretrained weights are published as GitHub Release
assets. This keeps a clone small and is enforced by CI, not just by `.gitignore`.

## License

MIT — see [LICENSE](LICENSE).
